name: security

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  hidden-state-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check realtime publish/send guard
        run: bash scripts/security/grep-no-leak.sh
